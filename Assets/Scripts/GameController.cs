using UnityEngine;

public class GameController : MonoBehaviour
{
    private int floor;
    private int traversedRooms;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        floor=0;
        traversedRooms=0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
